Reserved for RelicOS updates: a future release drops its update bundle
here from any PC, and the console applies it to the inactive system slot
on the next boot (A/B, M15+). Nothing reads this folder yet -- it exists
so the address is stable from day one. (This file also keeps the folder
in git, which does not track empty directories.)
