Reserved for device-tree overlays (ADR 0006): hardware variant fixes that
would otherwise need a reflash, applied at boot from this folder. The
mechanism is not wired up yet -- this folder exists so the address is
stable from day one. (This file also keeps the folder in git, which does
not track empty directories.)
